#!/usr/bin/env bash
# This script parses /etc/passwd to display username, uid, and home directory

while IFS=: read -r username password uid gid info home shell; do
    echo "${username}:${uid}:${home}"
done < /etc/passwd
