#!/usr/bin/env bash
# This script lists files in the current directory showing names after the first dash

for file in *; do
    if [ -f "$file" ] || [ -d "$file" ]; then
        echo "$file" | cut -d'-' -f2-
    fi
done
